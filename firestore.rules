rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Ajusta estas reglas antes de usar en produccion.
      allow read, write: if true;
    }
  }
}
